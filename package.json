{
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "node -e \"if(!process.env.CI)require('node:child_process').execSync('lefthook install',{stdio:'inherit'})\""
  },
  "lint-staged": {
    "*.{ts,tsx,md}": "prettier --write"
  },
  "devDependencies": {
    "lefthook": "2.1.9",
    "lint-staged": "17.0.7",
    "prettier": "3.8.4"
  }
}
